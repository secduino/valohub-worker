{
  "name": "valohub-worker",
  "version": "1.0.0",
  "description": "ValoHub Store Check Worker",
  "private": true,

  "main": "worker.js",

  "scripts": {
    "start": "node worker.js"
  },

  "engines": {
    "node": ">=18.0.0"
  },

  "dependencies": {
    "express": "^4.18.2",
    "node-cron": "^3.0.3",
    "node-fetch": "^2.7.0"
  }
}
